# Installed tools record: /.data/config/installed.tools.yaml

# This file is runtime generated by GACLI
# All read/write operations are delegated to parser.zsh

# Used by:
#   - update.zsh    → checks if update is due (compares installed.tools content with runtime dependencies values)
#   - modules.zsh   → populates this file, merging all modules tools.yaml files