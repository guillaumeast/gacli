# Core Brewfile: /.data/dependencies/core.Brewfile

# No read/write operations

# Used by:
#   - brew.zsh    → `brew bundle` with this file on first launch

brew "curl"         # HTTP(S) tool (macOS)
brew "wget"         # HTTP(S) tool (Linux)
brew "coreutils"    # Cross-platform tools
brew "yq"           # YAML processor
brew "jq"           # JSON processor

