###############################
# FICHIER /tools.yaml
###############################

# ────────────────────────────────────────────────────────────────
# Your custom tools
# ────────────────────────────────────────────────────────────────

# How does it work ?
# 1. Add or remove any formula, cask or module in this file
# 2. Save the file
# 3. Run "gacli update"

formulae:
  # List your custom HOMEBREW FORMULAE here
  # Or run `gacli add-form <formula_name>` / `gacli rm-form <formula_name>`
casks:
  # List your custom HOMEBREW CASKS here
  # Or run `gacli add-cask <cask_name>` / `gacli rm-cask <cask_name>`
modules:
  # List your custom GACLI MODULES here
  # Or run `gacli add-mod <module_name>` / `gacli rm-mod <module_name>`

