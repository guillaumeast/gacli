###############################
# FICHIER /tools.yaml
###############################

# ────────────────────────────────────────────────────────────────
# Your custom tools ✨
# ────────────────────────────────────────────────────────────────

# How does it work?
# 1. Add or remove any formula, cask or module below
# 2. Save the file
# 3. Run `gacli update` to apply changes

# Tip 💡 You can also use:
#   - `gacli add-form <formula>` / `gacli rm-form <formula>`
#   - `gacli add-cask <cask>`   / `gacli rm-cask <cask>`
#   - `gacli add-mod <module>` / `gacli rm-mod <module>`

formulae:
  # List your custom HOMEBREW FORMULAE here

casks:
  # List your custom HOMEBREW CASKS here

modules:
  # List your custom GACLI MODULES here

